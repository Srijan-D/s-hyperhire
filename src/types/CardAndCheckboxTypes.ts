export type CardDataFromAPI = {
  label: string;
  icon: string;
};

export type CardData = {
  label: string;
  icon: React.ComponentType | null;
};

export type CheckboxData = {
  label: string;
};
